export const metadata = {
  title: 'Meteor',
  description: 'Your AI powered video calling app',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
